application:
  db:
    username: ${DB_USER:dbuser}
    password: ${DB_PASS:password}
    r2dbc:
      host: ${DB_HOST:localhost}
      port: ${DB_PORT:3306}
      database: ${DB_NAME:db}
  reactive.duration: 60000
  reactive.limit: 100
  locale: "en_US"
  rest:
    user: "/user/api"
  security:
    strength: 12

logging:
  level:
    org.springframework: INFO
    su.svn: INFO

server:
  port: ${PORT:5000}

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${JDBC_DATABASE_URL:jdbc:mysql://localhost/db?reconnect=true}?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 1010
  profiles.active: production
  r2dbc:
    pool:
      enabled: true
      initial-size: 110
      max-size: 1010
  thymeleaf.mode: HTML
